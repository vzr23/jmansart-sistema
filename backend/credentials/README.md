# Credenciais Google Sheets

Coloque o arquivo JSON da Service Account nesta pasta com o nome:

```
service-account.json
```

**Service Account:** `jmansart-sheets@jmansart-cadastro.iam.gserviceaccount.com`

Certifique-se de que a Service Account tem permissão de **Editor** na planilha do Google Sheets.

> ⚠️ NUNCA commite o arquivo `service-account.json` no repositório. Ele já está no `.gitignore`.
